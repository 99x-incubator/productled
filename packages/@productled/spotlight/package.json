{
    "name": "@productled/spotlight",
    "version": "1.0.0",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "test": "jest",
        "clean": "rm -rf dist",
        "watch": "tsc -w"
    },
    "dependencies": {
        "@productled/tooltip": "^1.0.0"
    },
    "devDependencies": {
    }
}
